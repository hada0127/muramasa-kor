# TODO — 진행 예정 / 미해결 작업

> 완료 작업 상세는 [success.md](success.md), 실패·한계는 [fail.md](fail.md).
> 이 파일은 **열린/보류/예정** 항목만 둔다. 완료되면 success.md로 옮기고 여기선 지운다.
> 현재 릴리스: **v1.4.0** 🎉 (2026-06-06 — 파이썬 없이 쓰는 통합 GUI 설치 도구)
> 텍스처 편집: `python tools/ui_editor/server.py` (http://127.0.0.1:8765)

---

## ✅ 완료 — v1.4.0 통합 GUI 설치 도구 (2026-06-06, success.md 이관)

- 파이썬 없이 쓰는 Windows GUI exe + Vita3K/실기 통합 번들. 발행 완료(Latest). 상세는 success.md.
- 잔여 follow-up: ① 워크플로 actions Node20→24 버전업(무해 경고) ② Windows에서 exe 실제 더블클릭 동작
  최종 확인(사용자/메인테이너) ③ 차기 릴리스부터 통합 번들로 일원화 안내.

## 실기(real PS Vita) 패치 — v1.3.0 발행 완료, 실기 부팅 검증 대기 (2026-06-05)

- **✅ v1.3.0 발행**(main 머지, tag v1.3.0, gh release --latest, 실기 베타 zip 첨부).
  https://github.com/hada0127/muramasa-kor/releases/tag/v1.3.0
- CPK 내부 FTX에 한글 텍스처/폰트 직접 베이크 → 사용자 패처(`apply_realhw_patch.py`)로
  본인 CPK에서 생성 → `ux0/rePatch`+`reAddcont`. 상세는 success.md.
- **완료·검증**: 82/82 텍스처+폰트 매핑(high32, 누락 0), 커버리지 86/86 알파NCC>0.97, end-to-end
  CPK 라운드트립 한글 정상, clean-room zip OK. ETOC 무력화(실기 참고패치와 일치).
- **열린 항목(베타)**: 실기 부팅·표시는 **Vita3K 검증만**(macOS라 실기 불가). **사용자/제보자 실기
  테스트 필요** → 크래시면 ETOC 외 추가 대비(EnabledPackedSize/DataSize 갱신 등) 검토.
- **머지·릴리스 전 결정**: main 머지 시점, 버전 번호(v1.3.0?), gh release에 realhw zip 첨부 여부.

## 적용 완료·인게임 검증 대기 — 이슈 #16 (1.2.1 제보 2건, 2026-06-02)

제보자: mtmate-rgb (HD 텍스처팩 + ✕버튼 패치 동시 적용). 세이브 첨부. macOS라 인게임 검증은 제보자/사용자.

### #16-1 백사전 챕터선택 음영 어긋남 → ✅ 잔상 소거 (원인 확정)
- 원인(데이터 확정): 텍스처 `A3BE57CE9854B5CC`(idx4) K8 백사전. 원본 영문 "White Serpent"가
  K8 박스(x0-1281,y3314-3517)를 우측·하단으로 초과(글리프 x4~1516, y~3611). 한글 박스만 clear→
  박스 밖 영문 꼬리 **10,536px가 안 지워진 잔상**(어떤 clear박스에도 미포함). White Serpent가
  최장 타이틀이라 **백사전만** 발생.
- **codex 의견**: 잔상 소거 동의(넓은 단일 rect 말고 좁은 별도 clear-only, 텍스트 렌더 침범 주의).
  **추가 가설(발산)**: 주원인은 정렬 불일치일 수 있음 — 원본 "White Serpent Ninja Scroll"은 거의 한 줄로
  붙어 있는데 한글 백사전/인법첩은 각자 넓은 박스 중앙에 따로 렌더돼 **두 단어 사이가 크게 벌어짐** +
  보존된 白蛇 워터마크와 시각중심 어긋남. → 필요시 백사전 우측정렬/인법첩 좌측정렬로 붙이는 조정 제안.
- **agy 의견**: 가설(a) 잔상 지지(워터마크 정렬 가능성 낮음). 오버플로 영역 알파0 소거, 인접 K4 인법첩 손상 주의.
- **최종 조치**: K8 글리프(x≤870)·K4 인법첩(x≥1647)·K7 요도(x≤721) 사이 빈틈에만 clear-only 헬퍼
  region 추가(`K8_serpent_tail_clear`, clear_rect x1140 y3365 w395 h255). 재렌더 후 잔상 0px, 한글 0px 손실,
  백사전 글자 24,996px 보존 검증.
- **발산 처리**: 잔상은 데이터로 확정(미커버 10,536px 시각 확인)·소거 완료. codex의 정렬 가설은 인게임
  렌더 위치 의존이라 macOS서 검증 불가 → 이슈 코멘트로 "여전히 어긋나면 알려달라" 요청. 사용자 회신에
  따라 정렬(우측/좌측 붙이기) 추가 조정 여부 결정.

### #16-2 닌자주머니 메뉴 "반야탕" vs HUD "술" → ✅ "반야탕"으로 통일
- 원인: 메뉴=NMS텍스트 _itemdata id6 般若湯="반야탕". HUD 퀵슬롯=텍스처 `E8E01EAF5D41DB52`(idx5) K10,
  원본 영문 **"Sake"**→우리 "술". 원본 US부터 메뉴(般若湯)≠HUD(Sake) 이중구조. per-item 슬롯
  확인(hash8 甘酒="Sweet Rice Wine" 등) → "Sake"=般若湯 전용, 변경 안전.
- **codex/agy 의견 수렴**: 둘 다 (a) "반야탕"으로 통일 권장(아이템 정식명 일치로 버그 오인 방지 + 시대극 톤).
- **최종 조치**: K10 "술"→"반야탕"(fit_to_box로 박스 안착). config + ui_editor_index(text·native.text) 동기화.
  kr json 없음(라벨 참조파일 미존재). 재렌더 후 박스(w222) 내 x5-216 안착 검증.

### 인게임 검증 (2026-06-02, Claude가 직접 Vita3K 구동)
- 환경: macOS Vita3K v0.2.1. 버튼 매핑 SDL 스캔코드(config.yml): **Circle=C, Cross=X, Square=Z, Start=Enter**.
  **확인=X(Cross), 취소=C(Circle)** (사용자 ✕패치용 Enter=Cross). 메뉴 이동은 화살표(키 길게 눌러야 입력 안정).
  타이틀 진입: 부팅→어트랙트 무비 순환→X연타로 불러오기 메뉴 도달→상단 슬롯(사용자 세이브)→X로 로드.
- **#16-2 ✅ 인게임 확정**: 첨부 세이브 로드(키스케 제3막). 닌자 주머니에 "반야탕" 정상 표기 확인. 식품
  퀵슬롯에 般若湯 장착 후 게임플레이 복귀 시 우상단 HUD가 **"반야탕" x6**으로 표시(기존 "술"→해결).
  스크린샷 사용자 전송. (HUD는 아이템별 텍스처 슬롯: 甘酒→감주(hash8), 般若湯→반야탕(hash5 E8E01EAF K10).)
- **#16-1 ✅ 인게임 확정(codex 구동)**: 백사전 에피소드 선택 화면 캡처 성공(`/tmp/baeksajeon_ingame.png`).
  잔상 제거된 깨끗한 화면 확인. 픽셀 검증(잔상 0px·백사전 글자 24,996px 보존)+인게임 일치.
  **겐로쿠 괴기담 진입 경로(중요)**: 로고 후 "오보로 무라마사" 스플래시(밝은 종이배경)에서 **방향키 RIGHT**로
  본편→元禄怪奇譚(겐로쿠 괴기담) 패널 전환 → X로 진입 → 혜성 타이틀에서 X 여러 번(어트랙트 통과) →
  Select Play Style(無双/修羅) → 에피소드 선택 캐러셀(化猫/夜/백사전 등) → LEFT/RIGHT로 백사전에 맞추고
  **X 누르지 말고 캡처**(X=에피소드 시작). 입력 누락 방지: keyDown/keyUp 0.15~0.2s hold.
  macOS 권한 팝업 겹침 주의(허용 클릭 point≈(1290,454)).
- 세이브 백업/복원: 작업 전 내 세이브 /tmp 백업→사용자 세이브 로드→인게임 미저장→내 세이브 복원(디스크 원복).

### ★ 재진단·근본수정 (2026-06-04) — 06-02 "확정"은 오판이었음

- **06-02 캡처 재검증으로 오판 확인**: 당시 "깨끗하다"던 `/tmp/baeksajeon_ingame.png`를 다시 크롭하니
  자막이 **"백백사사전"으로 선명히 이중**(각 글자가 좌측으로 어긋나 겹침). 즉 06-02 잔상소거(A3BE만)는
  근본원인이 아니었고, 사용자가 "백사전만 미친듯이 어긋남"이라 한 그대로 이중현상이 남아 있었음.
- **근본원인 확정(두 텍스처·동일 단어)**: 겐로쿠 에피소드 상세화면은 **두 텍스처를 겹쳐** 그림 —
  ① `88A7A10233A61E85`(2048², 에피소드 제목 아틀라스: 큰 붓글씨 + 베이크된 소제목)
  ② `A3BE57CE9854B5CC`(4096², 장/무기명 아틀라스, K8 백사전).
  **오직 白蛇만** 두 텍스처의 텍스트가 똑같이 "백사전"이라 정확히 겹쳐야 하는데 어긋나 이중으로 보임.
  다른 3편은 88A7(혁명/네코마타/야생아)≠A3BE(대의를 위한 무 등)로 **서로 다른 두 줄**이라 무관.
  원본 영문판은 둘 다 "White Serpent"가 각 텍스처의 **같은 자리**라 완벽히 겹쳐 한 단어로 보였던 것.
- **측정(핵심 데이터)**: 원본 export의 "White Serpent" centroid 기준으로,
  A3BE kr 백사전 = (644,3415)/4096 ≈ 원본(640,3401) **정상**. 그러나
  88A7 kr 백사전 = (567,1264)/2048 으로 원본(501,1281)에서 **+66 텍스처px(≈18 Vita px) 우측 어긋남**.
  → 06-02가 손 안 댄 88A7이 진범. "미친듯이 어긋남"(큰 어긋남)과 부합.
- **최종 조치**: 88A7(수동 편집 넉아웃 텍스처)의 백사전 자막 픽셀을 직접 (-66,+17) 이동 →
  새 centroid (507,1277) ≈ 원본 위치. 한자 白蛇·이전 위치 잔상 없음 검증. A3BE는 정상이라 **미수정**.
  `tools/`엔 미반영(88A7은 생성스크립트 없는 manual). import + textures/kr/ui 동기화 완료.
- **검증 상태**: 진단은 06-02 캡처로 **증명됨**(A3BE만 고친 상태가 여전히 이중 → 88A7이 원인).
  단, 88A7 수정 후 인게임 AFTER 캡처는 macOS 자동화 한계로 미확보(창이 작고 AX 미노출, 겐로쿠 심층
  메뉴 진입 불안정). **사용자 인게임 최종 확인 필요.**

### ★★ 확정 실험 + 근본해법 (2026-06-04 추가) — 88A7 자막 제거

- **88A7 -66px 이동은 효과 없었음**(사용자 재시작 후에도 이중 그대로). → 단순 정렬로 안 풀림 확인.
- **확정 실험**: import에서 A3BE 백사전→우측 +250, 88A7 백사전→하단 +200 이동 후 사용자 캡처.
  결과: **금색 백사전 1개(=A3BE, 우측 이동본)** + **아래 어두운 조각(=88A7, 넉아웃 하단 이동본)**으로
  깔끔히 분리. → **두 텍스처 모두 자막을 그리고, 88A7이 중복 원인**임 100% 확정.
- **근본해법(채택)**: 88A7(에피소드 제목 아틀라스)의 **白蛇 BL사분면 백사전 자막만 제거**
  (한자 白蛇 y≤1164·다른 3편 자막 보존, y[1166:1360]×[0:1024] 알파255 흰판). A3BE의 단일 백사전만 잔존.
  - **왜 88A7을 지우나(A3BE 아님)**: 88A7 백사전 자막은 이 화면 전용. A3BE K8 백사전은 무기/장 이름
    화면에도 쓰여 지우면 그쪽 깨짐. 88A7 자막 제거가 안전(타 화면 무영향).
- **검증 대기**: A3BE 백사전 정상위치(K8 box center 640)가 한자 아래 잘 중앙정렬되는지 사용자 확인.
  살짝 치우치면 A3BE K8 region(config idx4)만 미세조정(쉬움).

### ★★★ 최종 방향 전환 (2026-06-04) — 삭제 철회, 위치 정렬 (사용자 지시)

- **사용자**: "지우지말고 위치를 맞춰야해. UI에디터 도입 전 히스토리엔 거의 맞았었어."
- **히스토리 추적으로 원인 확정**: UI에디터(7b01234, 05-22 05:29) **직후** "1박스=1단어 중앙정렬" 리워크
  (46da212~e938d8e)가 A3BE K8 백사전을 **박스중앙(cx 640)으로 이동 + 폰트 변경**. UI에디터 前
  (5d6f41f, 05-21)엔 백사전이 **cx≈582**(붓글씨체)라 88A7 자막(브러시)과 겹쳐 깨끗했음.
  88A7은 거의 안 변함(정규화 0.2717→0.2767). → **A3BE가 어긋난 주범**.
- **조치**: ① 88A7 baseline 복원(자막 있음, 삭제 철회). ② config K8 `w` 1281→1164(중심 640→582,
  pre-UI-editor 위치) — 재렌더 결과 cx=586 cy=3415, 잔상 0. ③ A3BE/88A7 import 동기화.
- **남은 가능성**: A3BE는 현재 깔끔한 Griun 폰트, 88A7은 붓글씨체 → 위치 맞아도 글자모양 달라
  살짝 겹침 블러 가능. 인게임 확인 후 필요시 A3BE K8을 pre-UI-editor 붓글씨 픽셀로 교체 검토
  (브러시 글리프 /tmp/A3BE_5d6.png 확보됨, 단 config 재렌더 시 덮어쓰임 — 내구성 처리 필요).

- **인게임 1차 확인(이미지5)**: 가로 정렬 양호("많이 좋아졌어"), 세로만 미세 어긋남(88A7 위/A3BE 아래).
- **세로 조정**: config K8 `y` 3314→3286, `h` 203→231 (clear 박스 위로 확장, 하단 3517 유지 →
  텍스트 중심 cy 3415→3401, pre-UI-editor 세로위치 일치). 재렌더 cx=587 cy=3401, K9·요도 온전, 영문잔상 0.

### ★★★★ 폰트 일치 (2026-06-04) — 붓글씨 글리프 스프라이트

- **인게임 2차(이미지6)**: 위치 거의 맞으나 "미묘하게 안 맞고 폰트도 다른 듯". → **폰트 불일치가 핵심**.
- **폰트 비교**: 88A7 자막 = 붓글씨체. pre-UI-editor A3BE 백사전도 **동일 붓글씨체**(서로 일치).
  현재 A3BE = Griun(가벼움), RIDIBatang(명조/세리프)도 불일치. **그 붓글씨 폰트는 repo에 없음**
  (.kra Krita 작업 때 쓴 폰트). → config 폰트 렌더로 재현 불가.
- **해법(내구성 O)**: ① pre-UI-editor 붓글씨 백사전 글리프를 `textures/sprites/baeksajeon_brush.png`(477×204)로
  추출. ② texture_localize에 **`glyph_image` region 기능 추가**(폰트 대신 PNG를 박스에 정렬 합성, clear는 그대로).
  ③ K8 config에 `"glyph_image"` 지정(RIDIBatang font 제거). 재렌더 cx=584 cy=3402, 재렌더 라운드트립 동일(내구성 OK).
- 이제 A3BE 백사전이 88A7과 같은 붓글씨체 + 같은 위치 → 겹쳐서 단일로 보여야 함.

### ★★★★★ Griun 통일 (2026-06-04, 사용자 지시 "그리운 경찰서체로 통일")

- 붓글씨 매칭 대신 **둘 다 Griun으로 통일**(사용자 선호). A3BE는 glyph_image 제거→Griun 폰트 렌더 복귀
  (cx588 cy3401 w515). 88A7(수동 텍스처)의 붓글씨 백사전을 **A3BE Griun 글리프로 교체**:
  A3BE 글리프(515×183)를 88A7 붓글씨 bbox(621×174)에 stretch-fit → 넉아웃 홀 카빙(한자·타 사분면 보존).
  - 화면 매핑: A3BE w515 ≈ 88A7 w620 = 같은 화면폭(사용자 "가로 맞음" 근거). 같은 Griun 글리프를 각
    아틀라스 스케일로 넣어 화면서 동일하게 겹침.
- glyph_image 기능(texture_localize)·붓글씨 스프라이트는 코드/자산으로 유지(미사용, 대안 보존).

→ 다음: 사용자 재확인 — Griun 통일로 단일 백사전이면 클로즈. 미세 위치차는 88A7 카빙 위치/A3BE box nudge.

## 보류 — DLC 엔딩 "完" → "완 결" 한글화 (이슈 후속, 2026-05-27 위치 확정)

- **출처 확정(codex+직접 조사)**: DLC 엔딩 完은 export/originals 텍스처가 아니라, 설치된 DLC 팩
  CPK 내부 `GUI/Ending_P1~P4.ftx`(에피소드별). 각 1024×1024 캐릭터 일러스트 + 좌하단 큰 붓글씨
  完(bbox≈x1-320,y430-850). 본편 엔딩 The End(`E4A9FD9D`)와 별개.
  - CPK: `~/Library/Application Support/Vita3K/Vita3K/fs/ux0/addcont/PCSE00240/OBOROMURAMASAPK1~4/NinPriPackN.cpk`
  - 추출본/미리보기(temp/): `temp/dlc_ftx_png/pack{1..4}/GUI/Ending_P*.png`, `temp/preview/DLC_endings_all4.png`
  - export 폴더(747개) 전수 — 일치 텍스처 없음 → **Vita3K import hash 미확보**.
- **방법(택1, 다음 작업)**:
  (A) 인게임에서 각 DLC 엔딩을 texture export 켜고 띄워 4개 hash 확보 → 完 자리에 완 결 오버레이.
  (B) DLC 팩 CPK의 `Ending_P*.ftx` 직접 교체(hash 불필요·전 사용자 적용). 단 **FTX 재인코더(DXT5+스위즐)
     신규 제작 필요** — 현재 repo엔 decoder `ftx_extract.py`만 있음.
- 사용자 결정: **이번 릴리스 보류.** 상세는 `docs/03-analysis/export-texture-audit-2026-05-20.md` "Ending 完 Handling".
- 참고: sysmsg 엔딩 텍스트(#255 본편·#279~282 DLC)는 이미 "완결"로 번역됨(텍스트는 정상).
- **2026-05-30 RGB 퍼셉추얼 재확인(claude+codex 병행)**: CPK 추출본 `Ending_P1~4.png`(1024², RGB 일러스트)를
  export(741개)+originals(92개)와 32×32 그레이 NCC 비교 → 최고 NCC **0.76**(동일=1.0 기준 한참 미달). 즉
  **export/UHD 어디에도 엔딩 텍스처 없음** 재확인. import hash 미확보 결론 변동 없음 → 인게임 export 캡처(방법 A) 필요.
  매칭 스크립트: `/tmp/rgb_fast.py`, 결과 `temp/ending_rgb_fast.json`.

## 보류 — DLC place 아틀라스 `place59`(pack3) 한글화 (2026-05-30 발견)

- `temp/dlc_ftx_png/pack3/GUI/place59.png`(512×256): 近江·伊賀·筑紫城·骸衆本陣·野鎚城 등 **여러 DLC 지명 묶음 아틀라스**.
- RGB 매칭에서 export/originals 1.0 매치 없음(최고 0.60) → **런타임 import hash 미확보**(엔딩과 동일 한계).
  개별 지명(近江/伊賀/筑紫/骸衆)은 이미 다른 등록 텍스처/대사에 한글 존재. 인게임 export로 hash 확보 시 등록 가능.
- 비교: place52~58·60~63(12개 중 11개)은 등록 originals와 NCC **1.000 동일** → 이미 등록·번역 완료.

## ✅ 해결 — 보스 드롭 "인왕의 팔찌→쌍나루코" = **실제 패치 버그였음** (2026-06-05 재조사·수정)

> ⚠️ 2026-06-01 "패치 무관 확정"은 **오판**이었다. 당시 `_itemdata` 인덱스를 JP NMS(3565개)
> 기준으로만 확인하고, 게임이 실제 로드하는 **US NMS(1177개)와의 인덱스 불일치**를 놓쳤다.
> 제보자(짜옹이)가 순정 ON/OFF 교차검증 + 스크린샷(영문 Bracelet of the Deva King ↔ 한글
> 쌍나루코 + "보스 키스케/모모히메 첫번째~세번째" 유령 아이템)으로 재제보 → 재조사로 버그 확정.

### 근본 원인
- 번역 데이터 `jp_messages.json._itemdata`는 **JP NMS 인덱스(3565개)** 기준 {id,ja,ko}.
- 게임이 로드하는 건 **US NMS(1177개)** — JP에만 있는 더미가 빠져 슬롯이 시프트됨:
  - JP 506~517 = `BOSS鬼助/百姫 N本目` 더미 6아이템(12슬롯), US엔 없음 → **+12 시프트**
  - JP 578~585 = `回復X1~4` 회복아이템(8슬롯), US엔 없음 → 누적 **+20 시프트**
- 기존 `build_patch.py` `us_itemdata_hybrid`가 `[0,594)`를 **JP idx i → US idx i 1:1**로 매핑 →
  US564(인왕의 팔찌 슬롯)에 jp[564].ko="쌍나루코" 주입. 액세서리 [506,564] 전체가 +12 어긋남 +
  BOSS 더미 6개가 액세서리 메뉴에 유령 항목으로 노출.

### 수정 (codex+agy 협의)
- **agy**: offset 규칙(+0/+12/+20) 채택·적용. **codex**: 앵커 회귀 검증 필수("조용히 틀리는" 버그).
  → **둘 결합**: offset 규칙 + 빌드 시 앵커 드리프트 가드(US506=鎖帷子/552=二繋ぎの鳴子/564=仁王の腕輪/566=琥珀の勾玉 불일치 시 빌드 중단).
- `tools/build_patch.py`: patch_patch `us_itemdata_hybrid` offset 보정 + patch_main `_itemdata_US`
  [506,566) +12 보정(base는 곡옥 없음). 양쪽 빌드 후 US564="인왕의 팔찌", 보스 더미 잔존 0 검증.
- 44개 액세서리 전수 영문↔한글 대조 정상.
- 참고: 한글 아이템명은 **일본어 원판 기준**이라 영문 공략명과 다름(인왕의 팔찌=Bracelet of the
  Deva King, 쌍나루코=Cowbell, 나루코=Triangle) — 이는 버그 아닌 번역 정책.
- **BOSS 더미 사실확인**(사용자 질문): `BOSS鬼助/百姫 N本目`는 효과 `敵用 所持/使用するな`(적 전용)
  = 적(보스)으로 등장 시 내부 데이터, 플레이어용 아님(영문판도 제거). "영문에선 oss로 표시" = 사실
  아님(oss는 boss battles 단어 일부). 공략의 "2회차 보스 클리어 후 사용가능 보스 아이템"은 별개 —
  반대/같은 캐릭터 보스 재격파로 얻는 **정상 드롭 액세서리**(인왕의 팔찌 등) 의미. 더미 숨김이 정답.
- **✅ v1.3.1 발행 완료** (2026-06-05): main push(228a069), tag v1.3.1, gh release --latest,
  5자산(vita3k-patcher/manifest/sha256 + xbutton + realhw-beta). 최종 배포 CPK 내부 US564=인왕의 팔찌
  검증. https://github.com/hada0127/muramasa-kor/releases/tag/v1.3.1 → 인게임 최종 확인은 사용자/제보자.

## 조사 종결 — export/UHD 미번역 지명·신규 텍스트 텍스처 없음 (2026-05-30)

- 계기: 사용자 발견 `FFFFD99DCD90D546` = 茶屋(찻집), 이미 place_jobs 등록·렌더 완료(status needs_review). 신규 아님.
- export(747)+UHD 팩(2139) 전수, 등록 82개 제외 후 텍스트 텍스처 탐지: claude 스코어러(419후보)·codex 스코어러(50후보)
  상위 후보 시트 **육안 검증 → 전부 구름/발광/파티클/이펙트**, 붓글씨 한자 지명 카드 0건. → 미번역 지명 텍스처 없음 확정.

## 의도적으로 하지 않는 항목 (사용자 결정)

- **#9-3 무기명 변수 치환**: 원작은 무기명을 런타임 변수(`##（무기）`)로 출력하나 US 엔진이 토큰을
  제거("this blade") → 텍스트/폰트 패치로 복원 불가(eboot 역공학 필요). **진행하지 않음.**

## 추후 필요 시 (현재 미예정)

- `_itemdata`/`sysmsg` 등 비대사 섹션 재번역 (필요해지면 대사와 같은 파이프라인으로).
- 손글씨체 重/隼 폰트 페이지(`font2a/2b` 계열)는 해당 화면 export 시 `auto_font_import` 재실행으로 자동 커버.
- 이슈 #11 후속(선택): UI 에디터에 실제 PIL 라이브렌더 표시(서버 `render_live` 연동)로 WYSIWYG화.
- 이슈 #10 borderline 1건: scemsg#941 "기치를 발휘"(頓智) — 사용자 확인 후 "재치를 발휘" 검토.

---
새 작업·이슈가 생기면 이 파일에 추가하고, 끝나면 success.md로 옮긴다.
