# TODO — 진행 예정 작업

> 완료 작업 상세는 [success.md](success.md), 실패·한계는 [fail.md](fail.md).
> 현재 릴리스: **v0.9.3** / 폰트: 완성형 2350자(河/重/隼 3페이지) — 아래 ✅ 참조.
> 텍스처 편집: `python tools/ui_editor/server.py` (http://127.0.0.1:8765)

---

## 🆕 대사 재번역·현대화 (이슈 #9, 2026-05-25 착수) — 현재 메인 작업

### 배경 / 목표
폰트가 955자 단일 페이지였을 때, 대사를 (a)일부 받침글자 대체표기 (b)대사창 폭 제약으로
단어 빼기·축약했다. 이제 **완성형 2350자 확보**로 글자 제약은 풀렸으니, 축약·대체된 대사를
**일본어 원문 느낌을 최대한 살려 재번역**한다.

**불변 제약 (반드시 지킴)**:
- 인명·지명·아이템 통일(`proper_nouns.json`: 인물 111·아이템 500·용어 15) 유지
- 대사창 폭 **27자/줄**, 줄 수 **최대 3줄**(4줄 금지, 3줄은 인게임 확정 후)
- 마침표 규칙 + 줄임표(…)·마침표(.) 뒤 같은 줄 문장 이어지면 공백 1칸
- 일본어 문장부호/가나/한자 잔존 0, 2350자 외 글자 0

### 참고 소스 (재번역 대조용)
| 소스 | 위치 | 정확도 | 비고 |
|---|---|---|---|
| 일본어 원문(ja) | `jp_messages.json` 보유 | ★★★ | **1차 소스** |
| 영문 공식번역(Aksys) | Fandom Wiki(muramasathedemonblade.fandom.com) | ★★★ | gemini가 읽어 정리 가능. 뉘앙스 대조 |
| 일본어판 Act별 대사 | 朧村正 @wiki (w.atwiki.jp/oboromuramasa) | ★★★ | gemini 확보 가능 |
| Wii 한글패치 | `wii/messages/kr` | ★★ | 본편 1117개만 |
| 유튜브 한글 팬번역 | @tabris17l (본편+DLC 전편, `[한글자막]`) | ★★ | **하드섭**(CC 아님) → ffmpeg+tesseract/비전 OCR로 추출 가능(확인됨). 보조 참고·선택 |

> 유튜브 메모: longplay 자동자막은 음성인식이라 부적합(실증). @tabris17l 번역은 화면 박힌
> 하드섭이라 yt-dlp CC 추출 불가 → 프레임 OCR 필요(작업량·정확도 고려, 타인 번역물이라 참고만).

### 재번역 파이프라인 (codex+gemini 수렴안)
1. **후보 선별·점수화**: `len(ko)/len(ja)` 낮음 + 대체표기/축약 흔적 + 이슈#8 항목 + Wii 대비 의미량 적음 → 점수순 정렬
2. **AI 재번역**: 고유명사 **placeholder 보호**(`<PN_001>` 치환→복원→조사 보정) + 컨텍스트(ja·현 ko·위키·앞뒤 대사·사전) + 제약(27자/줄, 최대 3줄, 사극 말투 일관) → **`translations/retranslation_candidates.json`**(중간 산출물, 원본 직접 수정 금지)
3. **기계 보정**: `apply_proper_nouns` + 조사 보정 + `fix_punctuation` + `reflow_dialogs` (필요시 `condense_dialogs`)
4. **자동 검수 `lint_dialogs.py`(신규)**: 폭/줄수 초과, 일본어 잔존, …·. 뒤 공백, 종결어미 마침표, 고유명사 표준명 불일치, SJIS 인코딩 실패, 대체표기 잔존, 말투 급변 의심 → CSV/JSON 리포트(error/warning)
5. **통과분만 적용** → 빌드 → 정적검증(OOR/SJIS audit, `patch_main`+`patch_patch` 양쪽)
6. **인게임 샘플 검증**: 변경 상위 / 폭 한계 / 3줄 / DLC 각 편 초반 / 고유명사 포함 / 받침 많은 글자

### 선행 작업 (착수 순서)
- [ ] **3줄(및 4줄) 대사창 표시 한계 인게임 확정** — 테스트 마커(27자×2/3/4줄) 임시 삽입 후 Vita3K 캡처. "3줄 허용/4줄 금지" 확정
- [ ] `lint_dialogs.py` 작성 (위 4번 규칙)
- [ ] 후보 점수 산출 스크립트
- [ ] 위키 스크립트 gemini로 확보(영문 공식 대조용) — 본편/DLC
- [ ] (선택) 유튜브 OCR 추출 파이프라인 PoC — @tabris17l 한 편 자막영역 크롭+OCR 품질 평가

### 우선순위 (점진 적용)
1. 본편(Wii 참고 가능) **샘플 ~50개로 파이프라인 검증** (placeholder·lint·빌드·인게임)
2. 손실 점수 상위 200개
3. DLC 4편 전체
4. 나머지 본편
5. 전수 자동 검수(lint) 통과

### 이슈 #8/#9 반영 항목
- **받침 빠짐**(#8): 폰트 2350자로 해결됨 → `lint`로 대체표기 잔존 0 확인
- **축약 복원**(#8 "임시 정리"분): 일부 완료(상닌→상급닌자, 규치→규칙, 불시말→사태, 유다→아부라다,
  "바람아래에"→"바람 부는 방향에", "이런 곳을 노리면"→"이런 상태에 있다가는"). **나머지 축약 대사 전수 복원**
- **#9-3 무기명 통칭**: 원작은 구체 무기명을 출력하는데 우리는 `##（무기）` 토큰을 "무기"로 일반화.
  US 엔진의 변수 치환 메커니즘 조사 필요 — **별도 과제**(난이도 높음)

---

## ✅ 한글 폰트 완성형 2350자 확장 — 완료 (2026-05-25, 커밋 b6a58eb)

- 발견: 폰트 FTX에 GXT 16페이지, SJIS 한자 페이지 3개(河 0x89CD/重 0x8F64/隼 0x94B9).
- 매핑 960→2350자(河 960+重 1024+隼 366), char_substitutions 폐기, 외곽선 1.5 복원.
- import 6개(河4+重 E690E190+隼 5F01AD86), `auto_font_import` page_base 자동 판별.
- 인게임 검증 완료(重 레벨맘빡땡 / 隼 레벨쭝쭸쭹). 상세 success.md, 문서 CLAUDE.md.
- 자투리: 손글씨체 重/隼(font2a/2b 계열)는 해당 화면 export 후 재실행으로 자동 커버 / 6706A53E ASCII-河 처리 확인

---

## 기타
새 작업·이슈가 생기면 이 파일에 추가한다.
