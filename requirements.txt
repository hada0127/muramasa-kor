# Core dependencies for muramasa-kor build pipeline

# Image processing
Pillow>=10.0
numpy>=1.24
scipy>=1.10  # texture_localize.py auto_align mode (connected-component sprite mask)

# Vita3K automation (Windows only)
pyautogui>=0.9.54
mss>=9.0
pywin32>=306; sys_platform == "win32"
